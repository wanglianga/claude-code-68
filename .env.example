# ---- 应用配置（容器内） ----
# 应用监听端口（容器内）
PORT=8080
# SQLite 数据目录（容器内，已挂载 named volume 持久化）
DATA_DIR=/app/data

# ---- 部署配置（docker compose 使用） ----
# 宿主机发布端口：访问入口 http://localhost:${CC_PUBLISH_PORT}
CC_PUBLISH_PORT=3001
# compose 项目名（并行环境隔离，一般由环境注入，无需修改）
COMPOSE_PROJECT_NAME=playground
